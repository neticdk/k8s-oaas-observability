apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opentelemetry-operator.fullname" . }}-test-connection"
  labels:
    {{- include "opentelemetry-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget-webhook
      image: busybox
      command: ['wget']
      args: ['{{ include "opentelemetry-operator.fullname" . }}:{{ .Values.webhookService.port }}']
    - name: wget-controller-manager
      image: busybox
      command: ['wget']
      args: ['{{ include "opentelemetry-operator.fullname" . }}:{{ .Values.controllerManagerMetricsService.port }}']
  restartPolicy: Never
